apiVersion: v1
kind: Service
metadata:
  name: orderservice
  namespace: orderservice
  labels:
    app: orderservice
spec:
  selector:
    app: orderservice
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
